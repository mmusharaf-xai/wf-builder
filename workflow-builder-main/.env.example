# Storage backend: IN_MEMORY (no DB server) | POSTGRES (uses PG_DB_URL)
STORAGE=IN_MEMORY

# Required when STORAGE=POSTGRES. Dummy URL is fine for `prisma generate` in IN_MEMORY mode.
PG_DB_URL="postgresql://user:pass@localhost:5432/workflow_builder"
